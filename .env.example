# FastAPI yt-dlp Service Configuration

# Server
PORT=8000

# CORS (comma-separated origins)
ALLOWED_ORIGINS=*

# Storage
DOWNLOADS_DIR=/tmp/downloads
FILE_TTL_SECONDS=300
CLEANUP_INTERVAL_SECONDS=60

# Logging
LOG_LEVEL=INFO

# YouTube Authentication (Optional but recommended for server deployments)
# Base64-encoded Netscape format cookies.txt from youtube.com
# How to get: Log into YouTube in an incognito window, use "Get cookies.txt LOCALLY"
# browser extension, export youtube.com cookies, then base64-encode the file:
# Linux/Mac: base64 -w 0 cookies.txt
# Windows: certutil -encode cookies.txt cookies_b64.txt (then remove header/footer lines)
YTDLP_COOKIES_B64=

# PO Token for additional bot detection bypass (optional)
# See: https://github.com/yt-dlp/yt-dlp/wiki/Extractors#po-token-guide
YTDLP_PO_TOKEN=
YTDLP_VISITOR_DATA=
